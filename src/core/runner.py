"""stub 배치를 실행해 To-Be 출력 CSV를 생성.

# === 인수인계 시 교체 포인트 ===
# 이 모듈은 시연용 stub 배치를 subprocess로 호출한다.
# 실 운영에서는 이 호출부를 진짜 Net COBOL 배치 호출로 교체한다.
# 입출력 계약(shell_id 입력, 생성된 CSV 경로 반환)은 유지할 것.

# TODO (T2-4): run_batch(shell_id: str, config: Config) -> Path
"""
